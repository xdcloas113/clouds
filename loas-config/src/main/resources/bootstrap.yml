spring:
  application:
    name: config-server
  security: #这里设置了。bootstrap里面要设置密码
      user:
        name: loas
        password: loas


#encrypt:
 # failOnError: false
  #keyStore:
   # location: classpath:keystore.jks
    #password: ${KEYSTORE_PASSWORD:foobar} # don't use a default in production
    #alias: test
